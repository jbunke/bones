Grammar:

  x Standard IO
    x read() statement
    x print(expr) statement
    x println(expr) statement

  x Distinguished void main(string[] prog_args) function as program entry point

  x array literals !BUT THEY AREN'T EXPRESSIONS!

  x non-trivial import statements
  x classes with callable functions
  - access fields in imported classes
  - instances / objects

  x casting for primitives
    x (bool)
      x string -> "true" & "false"
      x int -> 0 & 1
    x (int)
      x string -> Integer.parseInt
      x bool -> true: 1 & false: 0
      x float -> Math.floor
      x char -> ASCII code
    x (char)
      x int -> char for ASCII
    x (string)
      x bool
      x int
      x float
      x char
    x (float)
      x int

  x adding elements & removing elements from lists
  x indexing lists and arrays with non-integer literal expressions

  x random()

  x concatenation

  - error messages display file

  x disjunction


